<% provide(:title, @title) %>
<% if @favourites.any? %>
    <% @favourites.each do |favourite| %>
        <div class="row">
            <%= favourite.product.name %> - <%= favourite.product.price %>
        </div>
    <% end %>
<% else %>
    No favourites!
<% end %>