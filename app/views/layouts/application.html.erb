<!DOCTYPE html>
<html>
  <head>
    <title>TiendaCodigo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <%= link_to "Tienda Codigo", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "Catalog", root_path, class: "nav-link" %>
            </li>
            <% if user_signed_in? %>
            <li class="nav-item">
              IN FAV: <%= current_user.favourites.count %>
            </li>
            <% end %>
          </ul>
          <form class="d-flex">
            <% if user_signed_in? %>
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <%= current_user.display_name.nil? ? current_user.email : current_user.display_name %>
                  </a>
                  <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <%= link_to "Favourites", favourites_path, class: "dropdown-item" %>
                    <li><hr class="dropdown-divider"></li>
                    <%= link_to "Logout", destroy_user_session_path, :method => :delete, class: "dropdown-item" %>
                  </ul>
                </li>
              </ul>
            <% else %>
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                  <%= link_to "Login", new_user_session_path, class: "nav-link" %>
                </li>
              </ul>
            <% end %>
          </form>
        </div>
      </div>
    </nav>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>